<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="todo.ui">

    <application
        android:name="todo.model.MyApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_action_name"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_action_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Err_One">
        <activity
            android:name=".details.DetailsTodoActivity"
            android:exported="false"
            android:label="@string/title_activity_details_todo"
            android:theme="@style/Theme.Err_One.NoActionBar" />
        <activity
            android:name="todo.ui.splash.SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="todo.ui.home.MainActivity"
            android:exported="true">

        </activity>

    </application>

</manifest>